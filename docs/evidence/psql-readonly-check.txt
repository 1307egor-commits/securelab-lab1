# Read-only звірка даних у PostgreSQL (локальний стенд securelab, порт 54329)
# EF Core згенерував ідентифікатори у PascalCase, тому в SQL вони в лапках.

-- Деталі інциденту, які показує успішна гілка GET /api/incidents/{id}:
                  Id                  |                 Title                 | Severity | Status 
--------------------------------------+---------------------------------------+----------+--------
 20000000-0000-0000-0000-000000000003 | Перевірка журналу комп'ютерного класу | Low      | New
(1 row)

-- Розподіл за severity (те, що агрегує GET /api/incidents/severity-summary):
 Severity | count 
----------+-------
 High     |     1
 Low      |     1
 Medium   |     1
(3 rows)

-- Внутрішній коментар існує в БД, але НЕ входить у details response:
 AuthorDisplayName | IsInternal |                     Body                      
-------------------+------------+-----------------------------------------------
 Bob               | f          | Перевірив заголовки листа: SPF fail, домен за
 Alice             | f          | Журнал вивантажено, аномальний запис локалізо
 Admin             | t          | Внутрішня примітка: не розголошувати ім'я лаб
(3 rows)

